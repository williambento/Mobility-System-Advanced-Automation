/*package io.mobility;

import org.apache.logging.log4j.core.appender.ConsoleAppender;
import org.python.antlr.PythonParser.print_stmt_return;

import io.mobility.banco.AlphaBank;
import io.mobility.banco.ContaCorrente;
import io.mobility.componentes.Driver;
import io.mobility.empresa.Company;
import io.mobility.empresa.Sumo;
import io.mobility.posto.FuelStation;

public class App {
    public static void main(String[] args) {


        /*AlphaBank banco = new AlphaBank();
        ContaCorrente contaCompany = new ContaCorrente("Company", "2210");
        ContaCorrente contaFuelStation = new ContaCorrente("FuelStation", "2211");
        ContaCorrente contaDriver = new ContaCorrente("Drivers", "2210");
    
        banco.adicionarConta(contaCompany);
        banco.adicionarConta(contaDriver);
        banco.adicionarConta(contaFuelStation);
    
        Company company = new Company(contaCompany);
        FuelStation fuelStation = new FuelStation(contaFuelStation);
        Driver driver = new Driver(contaDriver);
        ContaCorrente dadosBancarios = company.getContaCorrente();
        System.out.println(dadosBancarios);*/
        
        
        /*Sumo s1 = new Sumo();
        s1.start();*/
        
        /*Company company = new Company("data/dadosMotoristas.txt", "data/dados.xml", "ID10");
        String[] rota = company.getItinerary();


        for (String ponto : rota) {
            System.out.println(ponto);
        } */
    /*}
}*/
